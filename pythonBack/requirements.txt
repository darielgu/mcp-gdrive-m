# Core Google GenAI SDK (Gemini API client)
google-genai>=0.3.0

# MCP client library (Model Context Protocol)
mcp>=0.1.2

# Environment variable management
python-dotenv>=1.0.1

# HTTP + WebSocket support (used by MCP / async clients)
httpx>=0.27.2
websockets>=12.0
fastapi>=0.115.0
uvicorn>=0.35.0
